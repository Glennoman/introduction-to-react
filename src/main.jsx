import React from 'react'
import ReactDOM from 'react-dom/client'
import TodoCase from './RenderItems'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TodoCase />
  </React.StrictMode>,
)
